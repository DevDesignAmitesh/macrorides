{
  "name": "main-code",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "migrate:db": "cd packages/db && npx prisma migrate dev -n 'init'",
    "generate:db": "cd packages/db && npx prisma generate",
    "build:db": "cd packages/db && pnpm build",
    "final:db": "pnpm run migrate:db && pnpm run generate:db && pnpm run build:db",
    "build:types": "cd packages/types && pnpm build",
    "build:packages": "pnpm --filter ./packages/** run build",
    "final:build:packages": "pnpm run build:db && pnpm run build:types && pnpm run build:packages",
    "build:be": "cd apps/http-backend && pnpm i && pnpm build",
    "build:vendor-app": "cd apps/vendor-app && && pnpm i && pnpm build",
    "start:vendor-app": "cd apps/vendor-app && pnpm start",
    "start:be": "cd apps/http-backend && pnpm start",
    "final:script:be": "pnpm i && pnpm run final:db && pnpm run final:build:packages && pnpm run build:be",
    "final:script:vendor-app": "pnpm i && pnpm run final:db && pnpm run final:build:packages && pnpm run build:vendor-app"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "turbo": "^2.6.3",
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@10.26.0"
}
