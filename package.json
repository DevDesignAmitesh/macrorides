{
  "name": "main-code",
  "private": true,
  "scripts": {
    "build:types": "pnpm --filter @repo/types build",
    "build:db": "pnpm --filter @repo/db build",
    "build:shared": "pnpm run build:types && pnpm run build:db",
    "build:packages": "pnpm -r --filter ./packages/* build",
    "build:be": "pnpm --filter http-backend build",
    "build:vendor-app": "pnpm --filter vendor-app build",
    "final:script:be": "pnpm run build:shared && pnpm run build:packages && pnpm run build:be",
    "final:script:vendor-app": "pnpm run build:shared && pnpm run build:packages && pnpm run build:vendor-app"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "turbo": "^2.6.3",
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@10.26.0"
}
