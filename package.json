{
  "name": "main-code",
  "private": true,
  "scripts": {
    "build:backend": "pnpm turbo run build --filter=http-backend",
    "build:vendor-app": "pnpm turbo run build --filter=vendor-app",
    "db:generate": "cd packages/db && npx prisma generate",
    "final:build:be": "pnpm run db:generate && pnpm run build:backend"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "turbo": "^2.6.3",
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=18"  
  },
  "packageManager": "pnpm@10.26.0"
}
